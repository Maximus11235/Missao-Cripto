[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "missao-cripto"
version = "0.1.0"
description = "Biblioteca educacional de funções matemáticas para criptografia"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["funcoes"]
